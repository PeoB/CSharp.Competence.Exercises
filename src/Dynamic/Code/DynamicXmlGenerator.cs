using System.Dynamic;

namespace Dynamic.Code
{
    public class DynamicXmlGenerator : DynamicObject
    {
        //Hint: to make this class cleaner, it is recomended to inherit from IDynamicMetaObjectProvider and create a backing type that does the heavy lifting
        //But for this example, this is fine
        //Hint: check the overrides
        
    }
}